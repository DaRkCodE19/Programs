#include <stdio.h>
#include<conio.h>
int main()
{
  int n;
  float a=0,i,s=1;
  clrscr();
  printf("Enter number of terms:");
  scanf("%d",&n);
  for(i=2;i<=n;i++)
  {
    a= a+(1/i);  
  }
    s=s+a;
  printf("Sol. = %f",s);
  getch();
}

